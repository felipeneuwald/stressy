# Binaries; `go build` in the repo root writes the module's name, /stressy.
/stressy
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary and coverage output
*.test
*.out

# Environment variables
.env

# GoReleaser output — `goreleaser release --snapshot --clean` writes every
# cross-compiled binary here. Committing them to a public repo is permanent.
dist/

# Local-machine state
go.work
go.work.sum
.DS_Store
.claude/
